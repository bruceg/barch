- Update design-notes

- Convert all timestamps, especially those used for incremental backups,
  to struct timeval or timespec.

Create:

--owner
--group

Extract:

--modification-time
--[no-]same-owner
--[no-]same-permissions / --preserve-permissions

- Validate the SOA parameters.

- unhandled file types:
    b -- block device
    c -- character device
